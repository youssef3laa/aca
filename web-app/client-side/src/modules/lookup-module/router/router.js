import lookupRecords from '../views/lookup-page'

const routes = [
    {
        path: '/lookup-records',
        name: 'lookup-records',
        component: lookupRecords
    }
]

export default routes
